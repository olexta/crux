# Description:  A superb, monospace bitmap font (for X11 and console)
# URL:          http://sourceforge.net/projects/terminus-font
# Maintainer:   Oleksii Tkachuk, oleksii dot tkachuk at gmail dot com
# Packager:     Anton Baranov, abaranov dot anton at gmail dot com
# Depends on:   xorg-bdftopcf
# Nice to have: xorg-mkfontscale xorg-mkfontdir fontconfig

name=terminus-font
version=4.36
release=2
source=(http://downloads.sourceforge.net/project/$name/$name-$version/$name-$version.tar.gz)

build() {
    cd $name-$version

    ./configure --prefix=/usr \
                --psfdir=/usr/share/kbd/consolefonts \
                --x11dir=/usr/share/fonts/X11/terminus
    make
    make DESTDIR=$PKG install

    # ter-v* console fonts consist of all available codepages
    rm -f $PKG/usr/share/kbd/consolefonts/ter-[^v]*
    # remove all non ISO10646-1/cp1251 fonts for X11
    rm -f $PKG/usr/share/fonts/X11/terminus/ter-[^cx]*
}
